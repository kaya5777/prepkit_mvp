<!DOCTYPE html>
<html>
  <head>
    <title>PrepKit MVP</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  </head>
  <body class="bg-gray-100 font-sans antialiased">
    <div class="flex min-h-screen">
      <!-- Sidebar -->
      <aside class="w-64 bg-white elevation-2 overflow-y-auto md:block hidden">
        <div class="p-6">
          <%= link_to root_path, class: "flex items-center gap-3 mb-8 text-blue-600 hover:text-blue-700 transition-all duration-200 p-2 rounded-lg hover:bg-blue-50" do %>
            <div class="w-10 h-10 bg-gradient-to-br from-blue-500 to-blue-600 rounded-full flex items-center justify-center elevation-1">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
            </div>
            <span class="font-bold text-xl">PrepKit</span>
          <% end %>

          <div class="mb-3">
            <h2 class="text-xs font-bold text-gray-600 uppercase tracking-wider px-3">履歴</h2>
          </div>
          <ul class="space-y-1">
            <% @sidebar_histories.each do |history| %>
              <li>
                <%= link_to history_path(history), class: "block p-3 rounded-xl hover:bg-blue-50 transition-all duration-200 elevation-hover group" do %>
                  <div class="font-semibold text-gray-800 group-hover:text-blue-600 transition-colors truncate text-sm">
                    <%= history.company_name.presence || '（会社名なし）' %>
                  </div>
                  <div class="text-xs text-gray-500 mt-1 flex items-center gap-1">
                    <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <%= history.asked_at&.in_time_zone('Asia/Tokyo')&.strftime('%m/%d %H:%M') %>
                  </div>
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
      </aside>

      <!-- Main Content -->
      <div class="flex-1 overflow-auto">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
          <!-- Flash Messages -->
          <% flash.each do |type, message| %>
            <% css_classes = case type.to_s
              when 'notice' then 'bg-green-50 text-green-800 border-green-200'
              when 'alert' then 'bg-red-50 text-red-800 border-red-200'
              else 'bg-yellow-50 text-yellow-800 border-yellow-200'
            end %>
            <div class="<%= css_classes %> border rounded-lg p-4 mb-6 flex items-start gap-3">
              <svg class="w-5 h-5 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
              </svg>
              <span><%= message %></span>
            </div>
          <% end %>

          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
